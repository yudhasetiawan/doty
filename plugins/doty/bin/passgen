#!/usr/bin/env bash

# Generate a secure secret and have it copied to your clipboard. If your platform
# supports hiding it from clipboard managers, that will be used.
#
# Examples:
#   passgen

set -o errexit
set -o pipefail
set -o nounset

pwgen -sync "${1:-48}" -1 | clip-copy --sensitive
